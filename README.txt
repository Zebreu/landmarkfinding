Finds landmarks and generates a path that uses them.

An ant colony algorithm and a genetic algorithm are both implemented.

Numpy is required, and matplotlib is optional (used for visualization of the paths and maps)